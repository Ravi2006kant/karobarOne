import 'package:flutter/material.dart';

class ProductQa extends StatelessWidget {
  const ProductQa({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}