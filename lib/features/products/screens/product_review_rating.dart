import 'package:flutter/material.dart';

class ProductReviewRating extends StatelessWidget {
  const ProductReviewRating({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
