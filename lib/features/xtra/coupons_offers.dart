import 'package:flutter/material.dart';

class CouponsOffers extends StatelessWidget {
  const CouponsOffers({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}